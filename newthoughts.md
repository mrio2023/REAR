### 核心需求总结
你希望基于带时间戳的金融图数据，以少数标记的诈骗子图为基础，训练一个社区提取器（extractor），核心目标是从可疑种子节点出发定位其所属的诈骗社区，关键要解决诈骗社区内部连接稀疏、时序数据利用、特征融入损失函数等问题。

### 核心思路与解决方案梳理
#### 1. 核心问题与针对性策略
- **诈骗社区特性**：内部节点直接连接极少（仅0.12%），孤立性强 → 解决方案是构建**一阶k-ego子图**（以目标节点为中心拓展一阶邻居），可将内部连接度提升至20%，转而学习**资金流转模式**而非节点本身的连接关系。
- **时序数据利用**：暂未找到有效时序嵌入方法，计划通过后续社区拓展环节补充利用，核心先聚焦资金流转特征。
- **特征融入损失函数**：
  - 交易频率：不考虑账户消亡，可定义为「单位时间内账户参与的交易次数（如按天/小时统计）」，或「相邻交易的时间间隔的倒数均值」，直接作为特征项纳入损失函数；
  - 金额停留时间：计算资金在账户的停留时长（下一笔转出与转入的时间差），作为特征融入；
  - 分布特征：放弃本福定律，构建交易金额/频率的经验分布函数，将实际分布与诈骗社区的经验分布的偏离度（如KL散度、均方误差）加入loss，量化特征异常。

#### 2. 模型训练与社区提取流程（分阶段）
1. **初始训练阶段**：
   - 基于标记的诈骗子图（含一阶k-ego拓展后的数据），以**F1值为核心损失函数**，训练初始的社区拓展器（extractor）；
   - 用该初始模型对可疑种子节点进行拓展，得到粗糙的初始诈骗社区。
2. **二次细分与精炼阶段**：
   - 对初始拓展的k-ego子图，用Louvain算法做社区二次细分（划分资金流转模式社区，非直接的犯罪组织）；
   - 计算细分后各社区与种子节点初始拓展社区的**编码相似率**（阈值暂设0.6），筛选Top-K相似社区；
   - 基于筛选后的相似社区数据，对extractor进行二次强化学习（RL）训练，得到精细化模型。
3. **最终拓展阶段**：用精细化模型对种子节点再次拓展，定位最终的诈骗社区。

#### 3. 实验验证设计
- 测试集：从已知非法社区中抽取25%的社区，随机选100个种子节点；
- 核心指标：种子节点拓展路径的F1值≥0.6（目标阈值）；
- 备选方案：若标记数据少，先通过Louvain/Lebian算法预分社区，再提取与已知诈骗社区相似的社区补充训练数据。

### 总结
1. 核心突破点：放弃直接挖掘诈骗社区节点连接，转而通过**一阶k-ego子图**挖掘资金流转模式，解决社区内部连接稀疏问题；
2. 损失函数设计：以F1为核心，融入交易频率/金额停留时间特征、交易分布偏离度，量化诈骗行为特征；
3. 训练流程：分「初始拓展→Louvain细分选相似社区→二次RL精炼→最终拓展」四步，兼顾标记数据少、社区异质性的问题，目标是种子节点拓展路径F1≥0.6。